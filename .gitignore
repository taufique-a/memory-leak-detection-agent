# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Test / coverage output
coverage/

# Investigation artefacts (these get big fast - heap snapshots are huge)
.memory-agent/
artifacts/
reports/
snapshots/
*.heapsnapshot
*.heapprofile
*.cpuprofile
traces/

# Scenarios written by the UI's component search. Regenerated on demand from
# the code, so committing them adds churn without adding information. The
# hand-written ones in scenarios/ are tracked.
scenarios/auto-*.json

# Playwright
playwright-report/
test-results/

# Saved browser sessions. These are LIVE CREDENTIALS - a storage state grants
# access to the application for as long as the session lasts. `scenario login`
# refuses to write anywhere that does not match these patterns.
*.auth.json
.auth/

# Logs
*.log
npm-debug.log*

# Editor / OS
.vscode/
.idea/
Thumbs.db
Desktop.ini
